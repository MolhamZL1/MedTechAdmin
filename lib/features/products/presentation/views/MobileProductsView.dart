import 'package:flutter/material.dart';

class MobileProductsView extends StatelessWidget {
  const MobileProductsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
