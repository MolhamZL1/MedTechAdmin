import 'package:flutter/material.dart';

class TabletProductsView extends StatelessWidget {
  const TabletProductsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
